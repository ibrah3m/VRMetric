<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vroverlay.metrics">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.oculus.permission.REPORT_EVENTS" />
    <uses-permission android:name="com.oculus.permission.REPORT_EVENTS_DEBUG" />
    <uses-permission android:name="oculus.render_notification" />
    <uses-permission android:name="com.oculus.permission.HAND_TRACKING" />
    <uses-permission android:name="com.oculus.vr.permission.DISPLAY_OVERLAY" />
    <uses-permission android:name="horizonos.permission.RIPC_CAN_REGISTER_SERVER" />

    <uses-feature android:glEsVersion="0x30000" />
    <uses-feature
        android:name="android.hardware.vr.headtracking"
        android:required="false"
        android:version="1" />
    <uses-feature
        android:name="oculus.software.handtracking"
        android:required="false" />


    <application
        android:name=".OverlayApplication"
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="VR Overlay"
        android:persistent="false"
        android:theme="@android:style/Theme.Translucent.NoTitleBar">

        <meta-data
            android:name="com.oculus.supportedDevices"
            android:value="quest2|questpro|quest3|panther" />

        <meta-data
            android:name="com.oculus.vr.appcompat.Theme"
            android:value="android:Theme.Black.NoTitleBar.Fullscreen" />

        <meta-data
            android:name="com.oculus.vr.appcompat.EnableBoundaryVisibility"
            android:value="true" />

        <service
            android:name=".OverlayService"
            android:exported="true"
            android:foregroundServiceType="specialUse">

            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vr_overlay" />

            <intent-filter>
                <action android:name="com.vroverlay.metrics.OverlayService" />
            </intent-filter>
        </service>

        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="com.oculus.intent.category.2D" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".overlay.UserBackgroundReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.oculus.vr.ACTION_USER_BACKGROUND" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".overlay.VRShellSettingsReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.oculus.vr.ACTION_SHELL_SETTINGS_CHANGED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
